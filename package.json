{
  "name": "maplibre-handles",
  "version": "1.0.0",
  "description": "A library for managing interactive handles in MapLibre GL maps",
  "main": "dist/index.js",
  "module": "dist/index.mjs",
  "types": "dist/index.d.ts",
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc",
    "dev": "tsc --watch",
    "example": "cd example && vite",
    "start": "npm run example",
    "build:example": "cd example && vite build",
    "prepare": "npm run build"
  },
  "keywords": [
    "maplibre",
    "maplibre-gl",
    "geospatial",
    "maps",
    "handles",
    "editing"
  ],
  "author": "",
  "license": "MIT",
  "dependencies": {
    "@turf/turf": "^7.2.0",
    "maplibre-gl": "^5.3.0"
  },
  "devDependencies": {
    "@types/maplibre-gl": "^1.13.2",
    "typescript": "^5.8.3",
    "vite": "^6.2.5"
  },
  "peerDependencies": {
    "maplibre-gl": ">=2.0.0",
    "@turf/turf": ">=6.0.0"
  }
}
